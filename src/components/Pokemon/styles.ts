import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    },
    img: {
        width: 100,
        height: 100,
    },
    div: {
      width: '100%',
      flexDirection: 'row',
      alignItems: "center",
      justifyContent: "center",
      flexWrap: "wrap"
    },
    textoBotao: {
        alignItems: 'center',
        justifyContent: 'center',
        color: "#fff",
        fontSize: 16,
    },
});
